name: Q5 rank1 validation
on:
  push:
    branches: ['codex/**']
  pull_request:
permissions:
  contents: read
concurrency:
  group: q5-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  q5:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONUNBUFFERED: '1'
      OPENBLAS_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python check_port.py
      - run: python check_reference.py --out results/local-reference.json
      - run: python check_q5.py --evidence results/q5 --reproduce
      - run: python report_q5.py --check
