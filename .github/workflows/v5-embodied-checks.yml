name: V5 embodied validation
on:
  push:
    branches: ['codex/**']
  pull_request:
permissions:
  contents: read
concurrency:
  group: v5-embodied-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  embodied:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONUNBUFFERED: '1'
      OPENBLAS_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python check_reference.py --out results/local-reference.json
      - run: python check_v5.py --evidence results/v5 --reproduce
      - run: python report_v5.py --check
