name: V3 slopetime validation
on:
  push:
    branches: ['codex/**']
  pull_request:
permissions:
  contents: read
concurrency:
  group: v3-slopetime-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  slopetime:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONUNBUFFERED: '1'
      OPENBLAS_NUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python check_v3_slopetime.py --evidence results/v3-slopetime --reproduce
      - run: python report_v3_slopetime.py --check
