# V3 — Plasticity as a fluctuation

Status: §9's first standalone test is complete; this candidate failed its
registered advancement criteria. See [V3-RESULTS.md](V3-RESULTS.md). No agent
integration or later queue item is enabled. The separately registered
mean-disagreement successor closed as tuning-inconclusive before confirmation;
see [V3-SLICE-1-RESULTS.md](V3-SLICE-1-RESULTS.md). A new wider-search follow-up
resolved that boundary but failed independent noise-increase detection controls;
see [V3-GAIN-FOLLOWUP-RESULTS.md](V3-GAIN-FOLLOWUP-RESULTS.md). No performance
confirmation or agent integration follows. A further persistent-detector test
passed passive observation but failed learning-coupled detection; see
[V3-PERSISTENT-RESULTS.md](V3-PERSISTENT-RESULTS.md). Its performance partition
remains unrun. A separately registered reference-detector follow-up passed
detection but failed independent learning advancement; see
[V3-REFERENCE-RESULTS.md](V3-REFERENCE-RESULTS.md). No default or agent integration
follows. The bounded-burst controller follow-up likewise failed independent
learning advancement; even its tuned perfect-timing oracle failed the full SGD
comparison. See [V3-BURST-RESULTS.md](V3-BURST-RESULTS.md). This finite-menu
experiment is closed. The subsequent change-triggered forgetting study also
closed learning-negative: matched-control and oracle adaptation gains failed the
full control/retention gate. See [V3-FORGETTING-RESULTS.md](V3-FORGETTING-RESULTS.md).
No integration follows. The interference-as-discovery follow-up then tested
§11.1 directly with a hidden-context ground truth: the discovered unit failed
to localize the context (AUC* identical to shuffled control) and conditioning
added nothing over random splits. See
[V3-DISCOVERY-RESULTS.md](V3-DISCOVERY-RESULTS.md). This finite-menu
experiment is closed without integration. The historical thesis follows.

**Evidence correction from v1 completion:** see `V1-RESULTS.md`. The old
0.946→0.385 ladder used a different task label from the current suite. It does
not establish catastrophic forgetting or a fixed-width limit on the current
shape-label task. Likewise, a tabular baseline with privileged state identity
beating a raw-observation agent does not isolate interference as the cause.
These remain motivating hypotheses, not demonstrated diagnoses. §9's standalone
experiment has now been run: it improves on Adam but loses to the simpler
control and misses noisy switches. The novelty and optimiser-wide claims below
are historical proposals, not conclusions established by this repository.

---

## 0. The move

V1 added mechanisms. V2 changes the substrate. Neither is novel and I have said
so plainly in the record.

V3 does not add a mechanism. It **deletes** most of the constants and derives
them from one measured quantity.

> **Learning rate is not a parameter. It is the local variance of the thing
> being learned.**

That is the whole claim. Everything below is consequence, experiment, or
falsifier.

**Superseded by §9.** The claim above survives, but its headline framing
("parsimony: six knobs, one quantity") does not — that framing is a physics
aesthetic and it collides with Adam. §9 replaces it with a stronger and more
falsifiable one: *two* timescales of variance, whose difference separates noise
from change. Read §9 before §2.

The precedent is physical, not neural. The fluctuation–dissipation theorem says
a system's response to a small applied force is fixed by its spontaneous
fluctuations at rest: χ = σ²/kT. You do not tune the response. You measure the
jitter and the response follows. Einstein's 1905 Brownian paper is the same
trick used the other way round — the noise was not a nuisance to be suppressed,
it was the measurement.

Our system currently suppresses and tunes its noise (`NOISE=0.35`). V3 reads it.

---

## 1. Why this system, specifically

Three of our own numbers say fixed constants are the disease:

- **HISTORY.md records five timescale-mismatch failures and five mechanisms that
  validated honestly and then turned harmful when the regime moved.** Both are
  one illness: a constant tuned in regime A, carried into regime B. More tuning
  cannot cure it. Derivation can.
- **`volatile-4` = 0.420, our worst task by a distance.** A fixed learning rate
  is *provably* wrong there — after a switch you want high plasticity, during a
  stable block you want low. There is no single correct value, which is exactly
  why tuning failed.
- **Capacity collapse 0.946 → 0.385 as combinations went 4 → 16.** New learning
  overwrites old because every synapse is equally plastic forever. A synapse
  that has settled should stop moving. Nothing in v1 lets it.

These are not three problems. They are one problem seen three times.

---

## 2. The equivalences

Each row is currently a separate mechanism carrying its own constant. Under V3
each is a reading of the same variable.

| v1 mechanism | v1 constant | v3 |
|---|---|---|
| learning rate | `LR=0.05` | plasticity ∝ local eligibility variance |
| exploration noise | `NOISE=0.35` | the action *is* a sample; exploration width = posterior width |
| replay priority | `HIPPO_WINDOW=20` (recency proxy) | replay what still fluctuates |
| sleep / consolidation | `SLEEP_REPLAY` schedule | consolidate when fluctuation collapses |
| homeostasis | `ETA_TH`, target rate | variance target replaces rate target |
| uncertainty signal (planned, step 8) | — | already present; nothing to add |

Six knobs, one measured quantity. **The success metric for V3 is the count of
hyperparameters deleted without performance loss.** That is an unusual metric
and a hard one to fake, which is the point.

---

## 3. Mechanism

Local, online, one extra state per synapse (rank-1 approximable — see PRD Q5,
which already costs 3× memory for eligibility and can share the factorisation).

Per synapse, track the running mean and variance of its own eligibility:

```
m_ij ← (1-α)·m_ij + α·e_ij
s_ij ← (1-α)·s_ij + α·(e_ij - m_ij)²
```

Plasticity is gated by it:

```
Δw_ij = η₀ · (s_ij / (s_ij + s₀)) · e_ij · NM
```

A synapse whose credit assignment has settled (low `s`) becomes rigid. One whose
credit is still ambiguous (high `s`) stays plastic. No schedule, no per-task
tuning, no separate consolidation pass.

**Action selection** samples from the vote distribution rather than adding fixed
noise. Wide votes → exploration; collapsed votes → commitment. This is Thompson
sampling, but arrived at from the substrate rather than imposed on it.

**Replay priority** becomes fluctuation, not TD error. This is a sharper
difference than it looks. TD-error priority replays an inherently stochastic
state *forever*, because it stays surprising. Variance priority stops replaying
it once the variance is aleatoric rather than epistemic — once you have learned
that it is genuinely random. `volatile-4` is precisely a task built out of
states like that.

---

## 4. Predictions, and what kills the thesis

1. **LR is deletable.** Variance-gated brainsim ≥ tuned-LR brainsim on `nway-8`
   and `lock-10` *with no per-task LR*. If it needs one, the thesis is dead.
2. **`volatile-4` moves the most.** The theory was proposed because of
   non-stationarity, so the biggest gain must appear there. If `volatile-4` does
   not improve, the thesis is wrong for the exact reason it was advanced.
3. **Automatic annealing.** Effective LR must fall over training with no
   schedule. Directly measurable; no excuse for not checking.
4. **Uncertainty is calibrated.** Variance on held-out compositional
   combinations must exceed variance on seen ones. Our held-out score is 0.002
   — confidently wrong. If variance does not flag those cases, "variance =
   epistemic uncertainty" is false and everything above collapses with it.
5. **Variance-priority replay beats TD-error-priority replay on `volatile-4`,
   and only there.** A win everywhere would mean I measured something else.

### The kill condition

The variance estimator has a window, α. **If performance is as sensitive to α as
it was to `LR`, we have renamed a hyperparameter, not removed one.** This must be
tested first and reported whether or not it is convenient. The defence, if it is
needed, is a spectrum of estimators at multiple α (synaptic cascade models,
Fusi & Drew 2005) rather than one — but a defence invented after a failed test
is a worse thing than the failure.

---

## 5. Prior art, stated honestly

Everything here has a relative. The parts are not the contribution.

- **Neural sampling** — Hoyer & Hyvärinen 2003; Buesing et al. 2011; Berkes et
  al. 2011 (spontaneous cortical activity matches the prior). *The
  representation claim is theirs.*
- **Synaptic plasticity as Bayesian inference** — Aitchison et al. 2021, Nature
  Neuroscience: synaptic learning rate ∝ posterior uncertainty. **This is the
  closest existing work and it has the core idea at the synapse level.**
- **Uncertainty-modulated learning rate** — Behrens 2007; Yu & Dayan; Kalman TD
  (Kakade & Dayan). *Theirs.*
- **Uncertainty-driven exploration** — Thompson sampling; Bootstrapped DQN
  (Osband 2016); deep ensembles (Lakshminarayanan 2017). *Theirs.*
- **SGD noise as temperature** — Chaudhari & Soatto. *Theirs.*

What is left: Aitchison's is a synapse-level theory fitted to plasticity data.
Osband's is exploration only. Berkes' is perception only. Behrens' is a fitted
Bayesian observer, not a mechanism. **Nobody has run the deletion experiment** —
one measured fluctuation replacing the learning rate *and* the exploration
temperature *and* the replay priority *and* the consolidation schedule
*simultaneously*, in a running agent, with an ablation record.

The novelty is unification and deletion, not a new mechanism. That is the right
shape to be aiming at. Einstein added no forces; he removed the ether and
absolute time.

---

## 6. Relationship to v2 and to steps 7–13

V3 is **orthogonal to the substrate rewrite**. Variance-gated plasticity is a
change to the update rule and can be tested in v1 this week. This is good news:
it decouples the novelty question from the continuous-time port, which was
never where novelty was going to come from.

It also **absorbs step 8**. Step 8 was "a local signal for whether a decision
was worth learning from," which I assessed as a rediscovery of advantage. Under
V3 that signal is not added — it is already the quantity driving everything else.

**Step 7a (external baselines) remains a hard prerequisite.** Every prediction
above is a comparison. Without tabular Q-learning on `lock`, logistic regression
on `nway`/`xor`, and a memory baseline on `tmaze`, "≥ tuned-LR brainsim" is a
comparison against ourselves and proves nothing to anyone else.

---

## 7. First experiment

Smallest decisive test, in order:

1. Instrument only: log per-synapse eligibility variance in current v1. Check
   prediction 4 (calibration on held-out compositional combos). **If variance is
   not calibrated, stop here** — the rest cannot work.
2. Sweep α alone with `LR` fixed. Establish the kill condition before building
   anything on top of it.
3. Replace `LR` with the variance gate. Run `nway-8`, `lock-10`, `volatile-4`,
   5 seeds, against the frozen reference table.

Step 1 is an afternoon and it is a real fork in the road.

---

## 8. Is this moving away from the brain?

Partly toward, partly away. Both directions are worth stating precisely, because
the "away" one is a real cost and not a quibble.

### Toward — the plasticity rule itself is textbook neuroscience

- **BCM (Bienenstock, Cooper & Munro 1982)** already gates plasticity on a
  second moment. The sliding modification threshold is θ_M ∝ ⟨y²⟩ — the
  time-averaged *squared* postsynaptic activity. A variance-like quantity
  controlling plasticity is not an import from physics; it is the oldest
  well-supported metaplasticity model there is.
- **Metaplasticity** generally — Abraham & Bear 1996, "plasticity of synaptic
  plasticity" — is the observation that prior activity changes how plastic a
  synapse is next.
- **Cascade synapses (Fusi, Drew & Abbott 2005)**: internal states make a
  synapse progressively harder to modify. "Settled → rigid" is their mechanism.
- **Aitchison et al. 2021** derives synaptic learning rate ∝ posterior
  uncertainty and fits it to plasticity data.
- **Prediction 3 has already been observed in vivo.** Churchland et al. 2010,
  *Stimulus onset quenches neural variability*, across many cortical areas — and
  variability falls with training. The automatic annealing V3 predicts is a
  measured cortical phenomenon.
- **Spine volumes fluctuate intrinsically**, largely independent of activity
  (Loewenstein, Kuras & Rumpel 2011). Per-synapse fluctuation is a real,
  measured quantity, not a modelling convenience.

### Toward — and we already half-built it

`ADAPTIVE=True` in v1 is described in the source as *"NE/ACh: volatility from
reward-rate change → LR, noise"*. That is V3 in miniature: a measured
fluctuation modulating learning rate and exploration. It validated. **V3 is
ADAPTIVE made local and given six jobs instead of two**, which means the class
of mechanism has already survived our own ablation record rather than arriving
untested.

There is a second, sharper argument sitting in our defaults. `ETA_TH=0.0`:
first-moment homeostasis was measured useless because **k-WTA already pins the
mean firing rate to exactly k/n**. If the first moment is clamped by the
architecture, it carries no information. The variance is the only free statistic
left to read. Our own ablation shut the first-moment door.

### Away — the parsimony is more physicist than neuroscientist

This is the honest cost. **The brain does not unify its uncertainty signals into
one scalar.** Yu & Dayan — which v1 already cites — explicitly *separates*
expected uncertainty (ACh) from unexpected uncertainty (NE) into two
neuromodulators with different roles. §2's "six knobs, one quantity" is a
parsimony aesthetic imported from physics. It is more unified than the organ.

Two smaller departures:

- **Per-synapse vs per-neuron.** BCM's threshold is postsynaptic and
  per-*neuron*; that is what the data support. §3 specifies a per-*synapse*
  variance of eligibility, which is a stronger and less evidenced claim about
  what a synapse can locally access.
- **Sampling is a live debate, not a fact.** "The action is a sample from the
  posterior" (Buesing, Berkes) competes with probabilistic population codes (Ma,
  Beck, Latham & Pouget), where uncertainty lives in the width and gain of a
  population rather than in temporal sampling. V3 picks a side.

### Reconciliation — declared in advance, not as a rescue

The brain-plausible form of V3 is a **small spectrum of estimators at different
α**, not one: fast and slow second moments, mapping onto the expected/unexpected
uncertainty split. That is exactly what §4's kill condition offers as a defence,
which is the danger — a defence invented after a failed test is worse than the
failure.

So it is committed to now, before anything runs:

> **The theory is "plasticity is gated by a measured local second moment."** The
> number of estimators (one, or a small fixed spectrum) is a design choice
> *within* the theory, and both variants are declared before the experiment. If
> a single estimator fails and a two-estimator version works, that is a result,
> not a rescue. If *neither* works, or if either needs per-task tuning of α, the
> theory is dead as stated in §4.

### Consequent change to §7

**Do the per-neuron (BCM-style) version first.** It is better evidenced, cheaper,
and needs no rank-1 factorisation. Per-synapse variance becomes the second
experiment, run only if the per-neuron one earns it.

---

## 9. The reframe: not brain-like, but brain-sourced

The constraint changes from *"is this how a brain works"* to:

> Take what works in a brain **that is not in LLMs or neural nets**, and judge it
> by whether it pushes things forward generally.

This is a better constraint and it changes the thesis. Two consequences.

### The §8 cost disappears

§8's main objection was that "six knobs, one quantity" is more unified than the
organ — the brain deliberately splits uncertainty across neuromodulators (ACh
expected, NE unexpected; Yu & Dayan). Under the old frame that was a departure
to be justified. Under the new frame it is not a cost at all; it is a free
design choice. Nothing there needs defending.

### But the parsimony framing dies anyway — because that headline is Adam

Confront this directly, because it is the strongest objection to V3 and it is
not a brain objection at all:

**Adam already gates the learning rate on a local second moment.** Its
denominator is √⟨g²⟩ — a per-parameter running second moment of the gradient.
Every neural net trained in the last decade does per-parameter
variance-modulated plasticity. "Learning rate from local variance" is not the
thing brains have and nets lack. It is in the default optimiser.

Except **the sign is opposite.**

| | statistic | high variance ⇒ |
|---|---|---|
| Adam | one second moment of the gradient | **smaller** step |
| V3 §3 | one second moment of eligibility | **larger** step |

Both are right, about different things. Adam reads variance as *measurement
noise* — aleatoric — so shrinking is correct. V3 reads it as *ignorance* —
epistemic — so growing is correct. A single estimator cannot tell them apart,
which means **Adam conflates them, and so did §3.**

### The actual claim

> **Adam has one second moment and always shrinks. Brains have at least two, at
> different timescales, and their difference decides whether to shrink or grow.**

Slow estimator ≈ expected uncertainty ≈ how noisy this has *always* been ≈ ACh.
Fast estimator ≈ current variability ≈ NE. Their relative excess is the gate:

```
gate = max(0, s_fast - s_slow) / (s_fast + s_slow + eps)
```

- stable and noisy → fast ≈ slow → gate ≈ 0. Do not chase the noise. *(Adam)*
- settled and quiet → both small → gate ≈ 0. Do not overwrite what is known.
- something just changed → fast ≫ slow → gate → 1. Become plastic now.

This is a **Kalman gain, P/(P+R), approximated by two EMAs per parameter and no
covariance matrix.** The Bayesian filtering world has had the process-noise vs
observation-noise split forever (Kalman-TD; Kakade & Dayan), and it is precisely
the aleatoric/epistemic distinction. What it has not had is a version cheap
enough to be a drop-in optimiser: two extra floats per parameter, no matrix.

Note it also **deletes `s₀`** from §3, which was a hand-set constant standing in
for observation noise. The slow estimator measures it instead.

### Why this explains our own worst number

`volatile-4` = 0.420. A volatile task is built from states that are *stochastic
but stable* sitting next to states that *just changed*. To a single-timescale
estimator these are identical — both look like high variance. That is not an
incidental weakness of v1; it is the exact failure this gate is shaped to fix,
and it is why prediction 2 (§4) is the one that matters.

### Why it passes the "pushes things forward" test

The claim is now legible to someone who does not care about brains:

> Optimisers have no non-stationarity story. Adam cannot distinguish a noisy
> gradient from a changed objective, so continual and online learning fall back
> on replay, resets, or regularisation. A per-parameter signal that separates
> the two would be a drop-in modification to Adam.

That is testable **outside brainsim entirely** — a small non-stationary
supervised benchmark, Adam vs two-timescale Adam, no spiking, no eligibility, no
replay. Which happens to solve the problem that has dogged this project since
step 7a: it is an external baseline that exists whether or not our agent is any
good.

### What brains have that nets lack — the general list

For the record, since the reframe invites the question. Ranked by how absent
they are from current practice:

1. **No train/test boundary.** Continuous learning during deployment, without
   catastrophic forgetting. Nothing in an LLM does this. Everything below serves
   it.
2. **Multiple plasticity timescales that decide what to overwrite.** ← V3.
3. **Offline consolidation.** Sleep, replay, compression into slow weights. No
   deployed analogue.
4. **Neuromodulation** — a global scalar broadcast that retunes the whole
   network's learning in one step. No analogue at all.
5. **Local credit assignment.** Here nets arguably *win*; this is a constraint
   we have accepted, not an advantage we have shown.

V1 and v2 sit mostly in 5, which is why the novelty answer kept coming back
thin. V3 sits in 2, which serves 1.

### Consequent changes

- **§4 prediction 1 is downgraded.** "LR is deletable" is no longer the headline
  test; deleting a knob is a parsimony win, not a capability win. The headline
  test is now prediction 2, `volatile-4`.
- **§4's kill condition is retired as stated.** The two-estimator version is no
  longer a fallback to be declared in advance — it *is* the theory. The
  single-estimator version is now the ablation, and it is predicted to fail on
  `volatile-4` specifically.
- **New first experiment, ahead of §7.** Non-stationary supervised toy, Adam vs
  two-timescale gate. No brainsim involved. Days, not weeks, and it is the
  external evidence this project has never had.

---

## 10. The queue: what else goes in v3

Six candidates, ranked. Two are attempts at something genuinely unthought; the
rest are consequences that fall out and happen to delete constants.

**Discipline note first, because it is the thing most likely to ruin this.**
HISTORY.md records the exact failure of shipping five mechanisms that each
validated honestly and then turned harmful when the regime moved. This section
is a **queue, not a plan**. Only §9's two-timescale gate is on the critical path.
Nothing below is built until the one above it has a number.

---

### 10.1 The differencing pair is the only operation

Every teaching signal in brainsim is already the same shape and I did not notice:

| signal | fast | slow |
|---|---|---|
| reward prediction error | `r` | `V(s)` |
| TD error | value now | value then |
| volatility gate (§9) | `s_fast` | `s_slow` |
| novelty | current input stats | running input stats |
| homeostasis | current rate | target rate |
| lateral inhibition / k-WTA | unit | pooled neighbourhood |

Six mechanisms, six pairs of constants, one function:

```
DiffPair(x; α_f, α_s) = EMA(x, α_f) - EMA(x, α_s)
```

**Claim: there is one operation — difference two leaky integrators — and
everything else is a choice of what to feed it.** Centre-surround is the pair in
space. Adaptation is the pair in time. Opponent processes are the pair across
channels. It is the most repeated motif in neural circuitry and we implemented
it six times by hand.

**Relation to predictive coding, which we shelved twice.** This is not a
re-litigation of step 6. Step 6 was shelved because *representation* was not the
bottleneck — the random encoder already generalised (held-out 0.825, gate 2
FAIL). This is about the *learning-signal generator*, not the encoder. And it is
a strictly cheaper claim than Rao–Ballard: predictive coding needs a hierarchy
and top-down generative predictions. Here the prediction is just the slow EMA of
the same signal. **No hierarchy, no top-down, no generative model.**

The sharp test: on whatever predictive coding was supposed to buy us, does
temporal differencing buy it without the hierarchy? `DESIGN-predictive-coding.md`
is the pre-registered control.

Falsifier: if collapsing the six into one primitive costs performance anywhere,
they are not the same thing and the unification is cosmetic.

---

### 10.2 Retrieval destabilises — reconsolidation as a continual-learning mechanism

The one I think is least likely to have been tried as a working mechanism.

Every ML system assumes **reading a weight is free and non-destructive.** The
brain does not: retrieving a memory makes it labile and it must be re-stabilised
(Nader, Schafe & LeDoux 2000). Recall *rewrites*. Nothing in ML has this, and the
inversion is interesting on its face — it means **unused memories are more stable
than used ones**, the opposite of weight decay.

Mechanism, using machinery we already have (eligibility marks what was used):

```
on use (eligibility above threshold):  synapse becomes labile
while labile:                          plastic, at a rate set by the §9 gate
after disuse:                          re-consolidates at its current value
```

The payoff is the interaction with §9:

- gate ≈ 0 during the labile window → it re-closes unchanged. **Recall is free.**
- gate high → it re-closes *updated*. **An old memory is corrected by new
  information without ever replaying the episode that created it.**

That second line is the thing continual learning cannot currently do. Updating
old knowledge normally requires replaying old data.

**Honest limit, and it improves the design.** Reconsolidation can only update
what you actually touch. Replay can update states you have not revisited. So
they are **complementary, not competing**: reconsolidation handles the visited,
replay handles the un-visited, and the gate decides which is needed. That is a
principled division of labour rather than two mechanisms piled up.

Falsifier: if retention is no better than plain online learning, this is just
catastrophic forgetting with extra state. Test on a rule-change task where states
recur — sample-efficiency of correction vs replay, with no episode storage.

---

### 10.3 Event boundaries are gate spikes — delete `HIPPO_WINDOW`

Open PRD question Q4: in continuous time there is no `done`, so what delimits an
episode for replay? `HIPPO_WINDOW=20` is a recency proxy chosen by hand.

**A surprise *is* a boundary.** Replay from the last gate spike. Humans segment
continuous experience at prediction-error peaks (event segmentation theory,
Zacks & Tversky), and this is not currently how replay buffers are delimited in
RL.

Deletes a constant, answers a live PRD question, and is testable in v1 this week
against the fixed window.

---

### 10.4 Read the environment's timescale off the variance spectrum

§9's stated kill condition is that α becomes the new tuned parameter. This is the
defence, and it is a real mechanism rather than a hedge.

Run k EMAs at geometrically spaced α (k ≈ 5, per neuron, not per synapse). The
variance measured across timescales **is the local autocorrelation spectrum**.
The break in that curve is the timescale at which the environment actually
changes. You do not tune it — **you read it off where the curve bends.**

Multi-timescale EMA differences are old (a fast-minus-slow EMA is literally
MACD). Reading the *break point* as a control signal for plasticity is the part
I have not seen.

---

### 10.5 Replay triggers on quiet, not on a clock

`SLEEP_REPLAY` is a scheduled phase, currently ablated to 0. When the gate is
near zero everywhere there is nothing to learn from the world — so that is when
to replay. **Consolidation is not scheduled; it is what idle is.**

Matches the biology directly: sharp-wave ripples and hippocampal replay occur in
quiet wakefulness and immobility, not on a clock. Deletes another constant. Small
and cheap; do it after 10.3.

---

### 10.6 Surprise-driven timestep — where v2 finally earns its keep

Every neural net spends identical compute on every input. A continuous-time
system does not have to. When the gate is low, nothing is changing, so **take
bigger timesteps**.

This is the argument that makes the v2 substrate worth building. The measured
event-driven saving is 5.4× from sparsity alone; coarsening time during quiet
periods multiplies it, and quiet is the common case. Adaptive computation time
exists (Graves 2016) but as a per-input decision in a discrete net, not as a
continuously varying clock rate in a running system.

Bridges v3 to v2. Do not start it until 10.1 or 10.2 has produced a number.

---

### Ordering

1. §9 gate on a non-stationary supervised toy vs Adam — **critical path, nothing
   else starts first**
2. 10.3 event boundaries (cheap, deletes a constant, v1 today)
3. 10.1 differencing pair (large deletion, low risk, mostly refactor + ablation)
4. 10.2 reconsolidation (highest novelty, highest risk)
5. 10.4 variance spectrum (only if α proves fragile)
6. 10.5 quiet-triggered replay
7. 10.6 adaptive timestep (v2 only)

---

## 11. Out of the box

Further out than §10, and further from evidence. Three swings, taken by asking
which unexamined assumption *simplifies* things when dropped. Two more are noted
and deliberately not proposed.

None of these jump the queue in §10. They are here because they are worth
writing down, not because they are ready.

---

### 11.1 Interference is a discovery signal, not a failure

**The assumption dropped: a weight is a number.**

Make each weight an interval — `(μ, σ)` — and change the update rule in one
place:

```
evidence agrees with μ   ->  σ shrinks   (confidence)
evidence pulls against μ ->  σ GROWS, μ barely moves
```

Conflicting evidence **widens** instead of overwriting. Nothing is destroyed;
the weight just becomes *"it depends."*

That word is the whole idea. **A contested weight is not damaged, it is telling
you a variable is missing.** The entire continual-learning literature treats
interference as damage to be prevented — EWC, synaptic intelligence, replay, all
of them protect old weights. The flip:

> Interference is the system detecting that its input is incomplete.

And the fix is not protection, it is **discovery** — and the discovery problem
comes with a free label:

> **What predicts which way this weight wants to move?**

That is an ordinary supervised problem whose target is the *sign of the pull*,
available locally, online, no teacher. Solve it, allocate a unit that fires on
that direction, let the contested weights condition on it, and σ collapses. The
new unit is a **context** — which is exactly the gain-field modulation already
argued for in the V2 PRD.

**Prior art, honestly.** Weight distributions are standard (Bayes-by-backprop,
variational BNNs) but are used for regularisation and uncertainty estimates, not
as a structural signal. The real ancestor is **cascade-correlation (Fahlman &
Lebiere 1990)**, which adds a unit correlated with the residual *error*. This is
finer: error decomposed *per weight*, so the new unit resolves a specific
ambiguity rather than mopping up global error — and it runs online with no
training phase.

**Falsifier:** on a task where a hidden context determines the mapping, the
contested-weight set must localise the *right* variable. If the discovered unit
is uncorrelated with the true hidden context, the signal is noise and this dies.

**Note it subsumes §9.** Interval width already *is* the uncertainty the
two-timescale gate estimates. If 11.1 works, §9 becomes an approximation to it —
which is a reason to do §9 first, since it is far cheaper and tells us whether
the underlying claim holds at all.

---

### 11.2 Network size is grown, not chosen

**The assumption dropped: structure is fixed.**

The same signal that tells a *synapse* how much to learn tells a *unit* whether
to exist:

```
never uncertain        ->  die   (nothing depends on it)
permanently contested  ->  split (it is being asked to be two things)
```

Network size stops being a hyperparameter and becomes **a readout of task
complexity.** That is a meaningful deletion — layer width and depth are among
the most-tuned numbers in all of ML, and neural architecture search attacks them
from the outside at enormous cost. This is a local rule costing one comparison
per unit.

It also explains a v1 number. Capacity collapsed 0.946 → 0.385 as combinations
went 4 → 16 with 80 units fixed. Under this rule that collapse is not a failure,
it is a **growth signal that had nowhere to go.**

**Falsifier:** grown networks must reach the same accuracy as the best
hand-sized network without being told the task, and must *stop* growing. Endless
growth is the obvious failure mode and is the first thing to check.

---

### 11.3 Forgetting is the default; persistence must be earned

**The assumption dropped: forgetting is a bug.**

Every method in continual learning tries to *prevent* forgetting. Brains forget
hard, and it is adaptive — forgetting promotes generalisation and flexibility
(Richards & Frankland 2017).

Invert the default:

> Everything decays. Evidence buys exemption.

Then continual learning is not a problem to be solved; it is the resting state,
and the question flips from *"how do we protect the old?"* to *"what has earned
the right to persist?"* — which the §9 gate already answers.

It composes cleanly with 10.2: retrieval makes a memory labile, and everything
is decaying anyway, so **what survives is what is both used and consistent.**
Nothing else has to be scheduled or protected.

**Falsifier:** it is one line and it will be tempting to call any result a
success. The test must be retention on the old task at *equal* plasticity on the
new one — a method that forgets less by learning less has done nothing.

---

### 11.4 A rider: cheap deliberate experiments

If weights are intervals, the action that teaches you most is the one touching
the widest ones. Pick it. That is a **local O(1) approximation to expected
information gain** — normally intractable in Bayesian RL — and it replaces
exploration noise with an actual experiment.

Only meaningful if 11.1 holds.

---

### Noted, and deliberately not proposed

Two ideas that survived the brainstorm but should not be built:

- **Knowledge in delays rather than strengths.** If what is learned is *when* a
  spike arrives rather than *how strong* it is, forgetting has a different
  geometry — structure is not overwritten the way a scalar is. Izhikevich's
  polychronous groups (2006) are the precedent. Real, under-explored, and too
  expensive to test cheaply. v2 has axonal delays for parallelism; this would
  make them the learned parameter.
- **Cross-modal latency as a physics teacher.** Two senses reporting one event
  must agree, which is a teacher needing no labels and no reward. The brain's
  version has something CLIP does not: the streams arrive at *different
  latencies*, and the offset that maximises agreement encodes distance and
  causality. Learning the alignment is learning physics. Requires embodiment —
  step 10 — so it is parked, not rejected.

---

## 12. Expectations, written before anything runs

`DESIGN-predictive-coding.md` set the house rule: write the prediction first, so
a result cannot be reinterpreted after the fact. Same here.

The bar is **"will running this teach us something either way"**, which is lower
and more useful than "is it novel". Graded on that:

| | clear win | informative if it fails? |
|---|---|---|
| §9 two-timescale gate vs Adam | ~50% | **yes** — a single moment being enough is itself a finding |
| 10.1 differencing pair | ~60% | **yes** — if it costs performance, *where* it costs tells us the six are genuinely different |
| 10.3 gate-spike boundaries | ~40% | yes, small — a tie means the window was never doing work |
| 11.1 interference as discovery | ~30% | **yes** — "did it localise the right hidden variable" has a constructible ground truth |
| 10.2 reconsolidation | ~20% | **no** — a null is ambiguous between the idea and the implementation |
| 11.3 forgetting as default | ~35% | weak — one line, and easy to fool yourself |

**10.2 is the strongest idea and the weakest experiment.** It should not be built
until it has a design whose failure means something.

### What has actually improved

Not the mechanisms — the *questions*. v1's defining weakness was that every
number was measured against our own floor, our own ceiling, our own ablations.
§9's first experiment does not involve brainsim at all: Adam vs two-timescale
Adam on a non-stationary supervised toy. That is the first question this project
has asked that someone outside it could check.

### The risk that would invalidate all of it

**Amended after reading `tasks.py` — see `DESIGN-tasks-v3.md`.** The framing
below (scale) is mostly wrong. The suite's horizon is adequate: `Volatile` holds
a mapping for 300 trials. What is missing is *structure*, and one gap is fatal to
the critical path: **every task in the suite has deterministic reward.** §9's
entire claim is that one estimator cannot separate noisy from changed — in a
suite with no noise that confusion does not exist, a two-timescale gate shows no
benefit, and we read "the idea is wrong" when it means "the task cannot express
it." Cheaper to fix than scale, and worse to miss.

Original framing, kept:

**Every effect here is about scale and time, and our tasks are 80 units and 30
ticks.** Non-stationarity, forgetting, capacity pressure, structural growth —
these are all phenomena of long horizons and large models. There is a real chance
that every mechanism in §10 and §11 is *invisible at our scale*, and we will read
that as "the idea is wrong" when it means "the instrument is too small."

That is HISTORY.md's most-repeated failure — six instruments measuring something
other than their name — pointed at the whole of v3 at once.

**Consequence: growing the tasks is a prerequisite, not a follow-up, and it is
not currently scoped anywhere.** It goes in front of everything except the §9
toy, which is the one experiment that escapes the problem by not using our tasks
at all.

### Honest summary

Enough to learn from: **yes.** The two-timescale claim is a real gap in
optimisers and a real thing brains do, and it is cheap to settle.

Enough to claim a breakthrough: **no,** and nothing here should be described as
one until the toy returns a number.
